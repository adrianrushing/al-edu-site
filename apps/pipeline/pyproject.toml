[project]
name = "pipeline"
version = "0.1.0"
description = "Legacy pipeline package with medallion commands removed"
readme = "README.md"
requires-python = ">=3.14"

[project.scripts]
eflt-pipeline = "pipeline.cli:main"

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[tool.ruff]
extend = "../../pyproject.toml"
